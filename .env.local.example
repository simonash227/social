# Upload-Post API key (get from Upload-Post dashboard)
UPLOAD_POST_API_KEY=your_key_here

# Local dev: database stored in ./data/ (Railway overrides via RAILWAY_VOLUME_MOUNT_PATH)
DB_PATH=./data/app.db
