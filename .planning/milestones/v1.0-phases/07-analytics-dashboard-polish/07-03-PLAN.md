---
phase: 07-analytics-dashboard-polish
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: []
autonomous: false
requirements: [ANLY-01, ANLY-02, ANLY-03, ANLY-04, DASH-01, DASH-02, DASH-03, DASH-05]

must_haves:
  truths:
    - "All dashboard pages render without errors"
    - "Analytics cron is registered and can be triggered"
    - "All requirement behaviors are visually or functionally verifiable"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete analytics and dashboard implementation.

Purpose: Confirm all 8 requirements are met end-to-end before marking Phase 7 complete.
Output: Verified phase with all requirements confirmed.
</objective>

<execution_context>
@C:/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-analytics-dashboard-polish/07-01-SUMMARY.md
@.planning/phases/07-analytics-dashboard-polish/07-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify analytics and dashboard end-to-end</name>
  <action>
Human verifies the complete analytics collection backend and dashboard pages:
- postAnalytics schema + migration applied
- collect-analytics.ts cron worker with engagement scoring + tier classification
- Cron registered at 6h interval
- Cross-brand home page with brand cards + weekly digest (DASH-01, DASH-05)
- Activity log page with filters (DASH-03)
- Brand detail page with quick stats + recent posts (DASH-02)
- Per-brand analytics page (ANLY-04)
  </action>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Visit http://localhost:3000/ -- verify cross-brand home shows brand cards with stats (published count, engagement, next scheduled). Verify weekly digest section exists.
3. Visit http://localhost:3000/activity -- verify activity log renders with entries. Try filter dropdowns (brand, level, type). Verify error entries are highlighted differently.
4. Visit http://localhost:3000/brands/{any-brand-id} -- verify quick stats section (published/scheduled/avg engagement/top tier counts) appears above brand identity. Verify recent posts section shows last 5 posts with status badges. Verify Analytics button in header.
5. Click the Analytics button on brand detail -- verify /brands/{id}/analytics loads with per-platform breakdown, tier distribution, and top performers table.
6. Verify empty states: if no analytics data collected yet, pages should show informative messages rather than blank or erroring.
7. Verify `npx tsx scripts/validate-analytics.ts` passes all tests.
8. Confirm cron is registered: hit http://localhost:3000/api/health and check server console shows 'collect-analytics' in the registered jobs list.
  </how-to-verify>
  <verify>Human confirms all pages load and render correctly</verify>
  <done>All 8 requirements verified: ANLY-01 through ANLY-04, DASH-01, DASH-02, DASH-03, DASH-05</done>
  <resume-signal>Type "approved" if all pages render correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Human confirms all dashboard pages render with real data (or appropriate empty states) and all 8 requirements are met.
</verification>

<success_criteria>
- All pages load without errors
- Brand cards show real stats
- Activity log filters work
- Analytics page shows engagement data (or graceful empty state)
- Validation script passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-analytics-dashboard-polish/07-03-SUMMARY.md`
</output>
