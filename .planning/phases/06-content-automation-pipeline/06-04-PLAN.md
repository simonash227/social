---
phase: 06-content-automation-pipeline
plan: 04
type: execute
wave: 3
depends_on: [06-02, 06-03]
files_modified: []
autonomous: false
requirements: [FEED-01, FEED-02, FEED-03, FEED-04, FEED-05, FEED-06, FEED-07, FEED-08, FEED-09, FEED-10, SPAM-01, SPAM-02, SPAM-03, SPAM-04, SPAM-05, SPAM-06, SPAM-07]

must_haves:
  truths:
    - "Full automation pipeline works end-to-end: add feed -> poll -> score -> generate -> schedule"
    - "Automation levels route correctly"
    - "Spam prevention enforces all rate limits"
    - "Feed auto-disables after consecutive failures"
    - "UI properly reflects feed status and configuration"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete content automation pipeline. Verify all FEED and SPAM requirements work end-to-end.

Purpose: This phase has complex interactions between cron jobs, AI scoring, quality pipeline, spam guard, and scheduling. Human verification ensures the full loop works as expected.

Output: Verified automation pipeline ready for production use.
</objective>

<execution_context>
@C:/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-content-automation-pipeline/06-01-SUMMARY.md
@.planning/phases/06-content-automation-pipeline/06-02-SUMMARY.md
@.planning/phases/06-content-automation-pipeline/06-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of content automation pipeline</name>
  <files></files>
  <action>
Present the verification steps below to the user and wait for their confirmation. Claude should ensure the dev server is running (`npm run dev`) and the health endpoint has been hit (`/api/health`) to initialize cron jobs before presenting verification steps.

What was built:
1. Schema migration with feed automation columns (consecutiveFailures, enabled, automationLevel, feedEntryId)
2. Spam guard module enforcing 7 rules (daily caps, hour gap, warmup, topic dedup, stagger, link ratio, hashtag counts)
3. Feed polling module with RSS parsing, URL dedup, batch Haiku relevance scoring, auto-disable after 10 failures
4. Auto-generate module processing queued entries through quality pipeline with automation level routing
5. Two new cron jobs (feed poll every 5 min, auto-generate every 15 min)
6. Feed management UI with add/configure/delete feeds, automation level config
7. Brand detail page "Feed Sources" navigation link
  </action>
  <verify>
Human follows the verification steps:

**Step 1: Start the dev server**
Run `npm run dev` and navigate to the application.

**Step 2: Verify Feed Management UI (FEED-01, FEED-02)**
1. Navigate to any brand's detail page
2. Verify "Feed Sources" button appears alongside Generate, Media, Carousels
3. Click "Feed Sources" -- verify the feeds page loads
4. Verify automation level selector shows 4 options (manual/semi/mostly/full)
5. Change automation level to "semi" -- verify it persists on page refresh
6. Add a test RSS feed (e.g., `https://hnrss.org/newest?count=5` or any active RSS feed)
7. Verify the feed appears in the list with Active status
8. Verify per-feed config controls work (poll interval, relevance threshold)
9. Try adding a YouTube channel feed URL -- verify type auto-detects
10. Delete a feed -- verify it's removed

**Step 3: Verify Feed Polling (FEED-03, FEED-04, FEED-05, FEED-10)**
1. Add a valid RSS feed with automation level set to "semi" or higher
2. Trigger the cron by visiting `/api/health` to ensure cron jobs are initialized
3. Wait up to 5 minutes for the poll-feeds cron to run (or check activity log)
4. Check the feed in the UI -- verify entry counts increase
5. To test auto-disable (FEED-10): add an invalid feed URL (e.g., `https://httpstat.us/500`)
6. Over time (or by checking the DB), verify the feed disables after 10 consecutive failures

**Step 4: Verify Auto-Generation (FEED-06, FEED-07, FEED-08, FEED-09)**
1. Ensure a feed has been polled and has relevant entries (relevanceScore >= threshold)
2. Set brand automation level to "semi"
3. Wait for auto-generate cron (every 15 min) or check activity log
4. Verify new posts appear as "draft" status (semi mode always creates drafts)
5. Change automation level to "mostly"
6. Verify posts with qualityScore >= 7 are auto-scheduled, others remain draft
7. Check the calendar page -- verify auto-scheduled posts appear

**Step 5: Verify Spam Prevention (SPAM-01 through SPAM-07)**
1. Verify daily post counts in activity log respect platform caps
2. Check that posts are spaced at least 1 hour apart on same platform
3. If same source generates posts for multiple platforms, verify 30-60 min stagger
4. For a brand with warmupDate set: verify reduced posting limits in first 2 weeks
5. Verify same source URL doesn't produce duplicate posts within 48 hours
6. Check activity log for any spam guard "not allowed" messages

**Step 6: Check Activity Logging**
1. Navigate to the activity log (if available) or check the database
2. Verify entries for: feed_poll, auto_generate, spam guard decisions
3. Verify errors are logged at 'error' level, normal ops at 'info'
  </verify>
  <done>User confirms all verification steps pass. Full automation pipeline works: add feed, poll entries, score relevance, generate through quality pipeline, route by automation level, enforce spam prevention, schedule or draft posts. Type "approved" or describe issues.</done>
</task>

</tasks>

<verification>
All 17 requirements (FEED-01 through FEED-10, SPAM-01 through SPAM-07) verified by human tester through the steps above.
</verification>

<success_criteria>
User confirms:
- Feed management UI works (add, configure, delete feeds)
- Feed polling discovers and scores new entries
- Auto-generate creates posts through quality pipeline
- Automation levels route correctly (semi=draft, mostly=conditional, full=auto)
- Spam prevention enforces rate limits
- Feed auto-disables after consecutive failures
- Activity log captures all automation activity
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-automation-pipeline/06-04-SUMMARY.md`
</output>
