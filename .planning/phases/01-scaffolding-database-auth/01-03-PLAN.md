---
phase: 01-scaffolding-database-auth
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/page.tsx
  - src/app/layout.tsx
  - src/components/app-sidebar.tsx
  - src/components/top-bar.tsx
  - src/components/theme-provider.tsx
autonomous: true
requirements: [DASH-04]

must_haves:
  truths:
    - "Dashboard has a left sidebar with navigation: Home, Brands, Calendar, Activity Log, Settings"
    - "Sidebar has a brand switcher dropdown with 'All Brands' option at top"
    - "Top bar shows AI_MODE indicator badge (green for testing, orange for production)"
    - "Top bar shows system health status dot"
    - "Dashboard is dark mode only (no light mode toggle)"
    - "Sidebar collapses to icons on mobile"
  artifacts:
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout wrapping all authenticated pages"
      contains: "SidebarProvider"
    - path: "src/components/app-sidebar.tsx"
      provides: "Navigation sidebar with brand switcher"
      contains: "Sidebar"
    - path: "src/components/top-bar.tsx"
      provides: "Top bar with AI_MODE badge and health dot"
      contains: "AI_MODE"
    - path: "src/components/theme-provider.tsx"
      provides: "Dark-only theme wrapper"
      contains: "ThemeProvider"
  key_links:
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/app-sidebar.tsx"
      via: "import and render"
      pattern: "AppSidebar"
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/top-bar.tsx"
      via: "import and render"
      pattern: "TopBar"
    - from: "src/app/layout.tsx"
      to: "src/components/theme-provider.tsx"
      via: "wraps children"
      pattern: "ThemeProvider"
---

<objective>
Build the dashboard shell — sidebar navigation, brand switcher, top bar with AI_MODE badge, dark-only theme. All authenticated pages render inside this shell.

Purpose: Every subsequent UI feature (brands, calendar, etc.) renders inside this shell. It must exist before building pages.
Output: Dashboard layout with sidebar, top bar, and dark theme. Home page placeholder.
</objective>

<execution_context>
@C:/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-database-auth/01-CONTEXT.md
@.planning/phases/01-scaffolding-database-auth/01-RESEARCH.md

<interfaces>
<!-- From Plan 01: shadcn/ui components available -->
Components available in src/components/ui/:
- sidebar.tsx (SidebarProvider, Sidebar, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarTrigger, SidebarFooter, SidebarHeader)
- card.tsx (Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter)
- button.tsx (Button)
- badge.tsx (Badge)
- avatar.tsx (Avatar, AvatarFallback, AvatarImage)
- dropdown-menu.tsx (DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem)
- separator.tsx (Separator)
- tooltip.tsx (Tooltip, TooltipContent, TooltipProvider, TooltipTrigger)

<!-- From Plan 01: Database available for brand list -->
From src/db/index.ts:
```typescript
export function getDb(): BetterSQLite3Database<typeof schema>
```

From src/db/schema.ts:
```typescript
export const brands = sqliteTable('brands', {
  id: integer().primaryKey({ autoIncrement: true }),
  name: text().notNull(),
  // ... other fields
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme provider and update root layout for dark-only mode</name>
  <files>
    src/components/theme-provider.tsx
    src/app/layout.tsx
  </files>
  <action>
    **Create `src/components/theme-provider.tsx`.**
    - Simple wrapper around next-themes ThemeProvider
    - Export a client component ('use client')
    - Pass through children and any props to next-themes ThemeProvider
    - Use the standard shadcn pattern from 01-RESEARCH.md

    **Update `src/app/layout.tsx`.**
    - Import ThemeProvider from '@/components/theme-provider'
    - Import globals.css (shadcn init should have set this up, verify it's imported)
    - Import the Inter font from 'next/font/google' (or use the local WOFF file already in public/fonts)
    - Add `suppressHydrationWarning` to the `<html>` tag (prevents hydration flash, per 01-RESEARCH.md Pitfall 4)
    - Wrap children with ThemeProvider: `attribute="class"`, `defaultTheme="dark"`, `enableSystem={false}`, `disableTransitionOnChange`
    - Keep existing metadata (title, description)
    - Apply Inter font class to body
    - Set body className to include 'antialiased'
  </action>
  <verify>
    <automated>npx tsx -e "
const fs = require('fs');
const layout = fs.readFileSync('src/app/layout.tsx', 'utf8');
if (!layout.includes('suppressHydrationWarning')) { console.log('FAIL: missing suppressHydrationWarning'); process.exit(1); }
if (!layout.includes('ThemeProvider')) { console.log('FAIL: missing ThemeProvider'); process.exit(1); }
if (!layout.includes('dark')) { console.log('FAIL: missing dark theme'); process.exit(1); }
if (!fs.existsSync('src/components/theme-provider.tsx')) { console.log('FAIL: theme-provider missing'); process.exit(1); }
console.log('Theme setup PASS');
"</automated>
  </verify>
  <done>
    - ThemeProvider wraps the app with dark-only mode
    - Root layout has suppressHydrationWarning on html tag
    - No theme toggle (dark mode only)
    - Inter font applied
  </done>
</task>

<task type="auto">
  <name>Task 2: Create dashboard layout with sidebar navigation, brand switcher, and top bar</name>
  <files>
    src/app/(dashboard)/layout.tsx
    src/app/(dashboard)/page.tsx
    src/components/app-sidebar.tsx
    src/components/top-bar.tsx
  </files>
  <action>
    **Create `src/components/app-sidebar.tsx`.**
    Server component that:
    - Queries brands from DB via getDb() to populate brand switcher
    - Uses shadcn Sidebar components (SidebarProvider, Sidebar, SidebarContent, SidebarHeader, SidebarMenu, etc.)
    - Navigation items in SidebarContent:
      - Home (icon: Home/LayoutDashboard) -> /
      - Brands (icon: Building2/Palette) -> /brands
      - Calendar (icon: Calendar) -> /calendar
      - Activity Log (icon: Activity/ScrollText) -> /activity
      - Settings (icon: Settings) -> /settings
    - Each nav item uses SidebarMenuButton with `asChild` wrapping a Next.js Link
    - SidebarHeader: Brand switcher dropdown using DropdownMenu
      - "All Brands" at top (links to /)
      - Then list each brand by name (links to /brands/{id})
      - Show currently selected brand name or "All Brands"
    - SidebarFooter: subtle version or app name
    - Use lucide-react icons (installed by shadcn)
    - The sidebar should use the shadcn sidebar's built-in collapsible behavior for mobile

    **Create `src/components/top-bar.tsx`.**
    Server component that:
    - Renders a horizontal bar at the top of the main content area
    - Left side: SidebarTrigger (hamburger menu for mobile) + current page breadcrumb or brand name
    - Right side:
      - AI_MODE badge: reads `process.env.AI_MODE ?? 'testing'` server-side. Green variant badge for 'testing', orange/destructive for 'production'. Text shows the mode value.
      - System health dot: small colored circle (green for now, could be dynamic later). Tooltip showing "System healthy" on hover.
    - Use shadcn Badge, Tooltip components

    **Create `src/app/(dashboard)/layout.tsx`.**
    - Import SidebarProvider from shadcn sidebar
    - Import AppSidebar and TopBar
    - Render: SidebarProvider wrapping AppSidebar + main content area
    - Main content area: TopBar at top, then {children} in a padded container
    - Use the standard shadcn sidebar layout pattern:
      ```
      <SidebarProvider>
        <AppSidebar />
        <main className="flex-1 flex flex-col">
          <TopBar />
          <div className="flex-1 p-6">{children}</div>
        </main>
      </SidebarProvider>
      ```

    **Create `src/app/(dashboard)/page.tsx`.**
    - Simple placeholder home page with "Welcome to Social Content Engine" heading
    - Card showing basic instructions or next steps
    - This page will be replaced with the cross-brand dashboard in Phase 7 (DASH-01)

    **Move or update `src/app/page.tsx`** if needed. The root page.tsx should redirect to / which is handled by the (dashboard) group. Since (dashboard) is a route group (parentheses), its page.tsx IS the root page. Delete or update the existing src/app/page.tsx so it doesn't conflict. The (dashboard)/page.tsx should serve as the home page at path /.
  </action>
  <verify>
    <automated>npx tsx -e "
const fs = require('fs');
const files = [
  'src/app/(dashboard)/layout.tsx',
  'src/app/(dashboard)/page.tsx',
  'src/components/app-sidebar.tsx',
  'src/components/top-bar.tsx'
];
for (const f of files) {
  if (!fs.existsSync(f)) { console.log('MISSING:', f); process.exit(1); }
}
const sidebar = fs.readFileSync('src/components/app-sidebar.tsx', 'utf8');
if (!sidebar.includes('Brands') || !sidebar.includes('Calendar') || !sidebar.includes('Activity')) {
  console.log('FAIL: sidebar missing nav items'); process.exit(1);
}
const topbar = fs.readFileSync('src/components/top-bar.tsx', 'utf8');
if (!topbar.includes('AI_MODE')) {
  console.log('FAIL: top bar missing AI_MODE'); process.exit(1);
}
const layout = fs.readFileSync('src/app/(dashboard)/layout.tsx', 'utf8');
if (!layout.includes('SidebarProvider') || !layout.includes('AppSidebar')) {
  console.log('FAIL: dashboard layout missing sidebar'); process.exit(1);
}
console.log('Dashboard shell PASS');
"</automated>
  </verify>
  <done>
    - Dashboard layout wraps all authenticated pages with sidebar + top bar
    - Sidebar has 5 navigation items: Home, Brands, Calendar, Activity Log, Settings
    - Brand switcher dropdown in sidebar header with "All Brands" option
    - Top bar shows AI_MODE badge (green for testing, orange for production)
    - Top bar shows system health indicator
    - Sidebar collapses on mobile via shadcn sidebar built-in behavior
    - Home page renders placeholder content
    - Dark mode only (no light mode flicker)
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- Dashboard pages render inside sidebar + top bar shell
- AI_MODE badge shows correct color based on env var
- Navigation links point to correct routes
- Mobile sidebar collapses/expands via trigger button
</verification>

<success_criteria>
- Dashboard shell is the layout for all authenticated pages
- Sidebar navigation works with proper routing
- Brand switcher queries brands from database
- AI_MODE indicator is always visible in top bar
- Dark mode renders without flash or hydration warnings
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-database-auth/01-03-SUMMARY.md`
</output>
