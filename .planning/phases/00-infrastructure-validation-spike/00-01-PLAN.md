---
phase: 00-infrastructure-validation-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .env.local.example
  - .gitignore
  - .node-version
  - scripts/validate/01-sqlite-wal.ts
  - scripts/validate/02-better-sqlite3-nextjs.ts
  - scripts/validate/03-cron-singleton.ts
  - scripts/validate/04-satori-sharp.ts
  - scripts/validate/05-upload-post.ts
  - public/fonts/.gitkeep
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "SQLite opens with WAL mode, passes integrity check, and creates -wal/-shm files"
    - "better-sqlite3 loads and executes queries in a Node.js context"
    - "node-cron singleton guard prevents double-registration on repeated calls"
    - "Satori renders JSX to SVG and sharp converts SVG to PNG file"
    - "Upload-Post API validation script structure is ready (runs with API key)"
  artifacts:
    - path: "scripts/validate/01-sqlite-wal.ts"
      provides: "SQLite WAL mode + integrity check validation"
    - path: "scripts/validate/02-better-sqlite3-nextjs.ts"
      provides: "better-sqlite3 load + query validation"
    - path: "scripts/validate/03-cron-singleton.ts"
      provides: "node-cron singleton guard validation"
    - path: "scripts/validate/04-satori-sharp.ts"
      provides: "Satori + sharp PNG rendering validation"
    - path: "scripts/validate/05-upload-post.ts"
      provides: "Upload-Post API request_id tracking validation"
    - path: "package.json"
      provides: "Project scaffold with all validation dependencies"
  key_links:
    - from: "scripts/validate/01-sqlite-wal.ts"
      to: "better-sqlite3"
      via: "import Database from 'better-sqlite3'"
      pattern: "pragma.*journal_mode.*WAL"
    - from: "scripts/validate/04-satori-sharp.ts"
      to: "public/fonts/"
      via: "font file read for Satori rendering"
      pattern: "readFileSync.*fonts"
---

<objective>
Scaffold a minimal Next.js 15 project and create all 5 local validation scripts for infrastructure assumptions.

Purpose: Validate SQLite WAL, better-sqlite3, node-cron singleton, Satori+sharp rendering, and Upload-Post API tracking locally before deploying to Railway.
Output: Working project with 5 standalone validation scripts that each exit 0 (pass) or 1 (fail).
</objective>

<execution_context>
@C:/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-infrastructure-validation-spike/00-CONTEXT.md
@.planning/phases/00-infrastructure-validation-spike/00-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project and install validation dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, .env.local.example, .gitignore, .node-version</files>
  <action>
Initialize a Next.js 15 project in the repo root:

1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --no-import-alias --yes` (use current directory since it's the repo root). If the directory isn't empty, initialize manually with package.json.

2. Install validation dependencies:
   ```
   npm install better-sqlite3 drizzle-orm satori sharp node-cron
   npm install -D @types/better-sqlite3 @types/node-cron tsx
   ```

3. Create `.node-version` with content `22` (pin Node 22 for Railway Railpack).

4. Create `.env.local.example` with:
   ```
   # Upload-Post API key (get from Upload-Post dashboard)
   UPLOAD_POST_API_KEY=your_key_here

   # Local dev: database stored in ./data/ (Railway overrides via RAILWAY_VOLUME_MOUNT_PATH)
   DB_PATH=./data/app.db
   ```

5. Ensure `.gitignore` includes: `.env.local`, `data/`, `node_modules/`, `.next/`, `test-output/`.

6. Create `next.config.ts` with minimal config (no manual serverExternalPackages needed -- better-sqlite3, sharp, node-cron are auto-externalized in Next.js 15).

7. Create `scripts/validate/` directory.

8. Download Inter font for Satori: `mkdir -p public/fonts && curl -L -o public/fonts/Inter-Regular.ttf "https://github.com/rsms/inter/raw/master/fonts/truetype/Inter-Regular.ttf"`. If curl fails, use an alternative source or create a placeholder note.
  </action>
  <verify>
    <automated>node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; const needed = ['better-sqlite3','satori','sharp','node-cron','tsx']; const missing = needed.filter(d => !deps[d]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All deps present');"</automated>
  </verify>
  <done>Next.js 15 project initialized with all validation dependencies installed. .node-version set to 22. .env.local.example created. scripts/validate/ directory exists. Inter font downloaded for Satori tests.</done>
</task>

<task type="auto">
  <name>Task 2: Create all 5 validation scripts</name>
  <files>scripts/validate/01-sqlite-wal.ts, scripts/validate/02-better-sqlite3-nextjs.ts, scripts/validate/03-cron-singleton.ts, scripts/validate/04-satori-sharp.ts, scripts/validate/05-upload-post.ts</files>
  <action>
Create 5 standalone validation scripts in `scripts/validate/`. Each script:
- Runs with `npx tsx scripts/validate/XX-name.ts`
- Prints `[PASS]` or `[FAIL]` prefix on every assertion line
- Exits 0 on success, 1 on failure
- Includes timing (start/end with elapsed ms)
- Is self-contained (no shared test utilities)

**01-sqlite-wal.ts (INFRA-01):**
- Use DB_PATH env var, default to `./data/test-validation.db`
- Create directory if needed with `fs.mkdirSync(path.dirname(dbPath), { recursive: true })`
- Open database with better-sqlite3
- Set `PRAGMA journal_mode = WAL` and verify result is 'wal'
- Set `PRAGMA foreign_keys = ON` and `PRAGMA busy_timeout = 5000`
- Run `PRAGMA integrity_check` and verify result is 'ok'
- Create a test table, insert a row, verify -wal and -shm files exist at `dbPath + '-wal'` and `dbPath + '-shm'`
- Clean up: close db, delete test db files (db, -wal, -shm)
- Print timing info

**02-better-sqlite3-nextjs.ts (INFRA-02):**
- Verify better-sqlite3 loads (`import Database from 'better-sqlite3'`)
- Create in-memory database (`:memory:`)
- Create table, insert, query, verify data round-trips correctly
- Verify the module reports its version/path
- This validates the native module compiles and loads -- the Railway build validation happens in Plan 02

**03-cron-singleton.ts (INFRA-03):**
- Simulate the instrumentation.ts singleton pattern
- Define a `register()` function with `globalThis.__cronRegistered` guard
- Call `register()` three times (simulating hot reload)
- Verify node-cron `schedule()` was called exactly once (use a counter)
- Verify the NEXT_RUNTIME guard logic: when NEXT_RUNTIME is not 'nodejs', register() should return early
- Do NOT actually start cron jobs -- just validate the guard pattern works

**04-satori-sharp.ts (INFRA-04):**
- Load Inter font from `public/fonts/Inter-Regular.ttf` (fail with clear message if font missing)
- Render a test slide with Satori: 1080x1080, dark background, white text "Validation Test", brand-style layout
- Convert SVG output to PNG with sharp
- Write PNG to `test-output/validation-test.png`
- Verify file exists and is > 1000 bytes (sanity check it's a real image)
- Print file size in bytes
- Clean up: delete test-output directory

**05-upload-post.ts (INFRA-05):**
- Read UPLOAD_POST_API_KEY from env (from .env.local)
- If no API key, print `[SKIP] UPLOAD_POST_API_KEY not set -- set in .env.local to validate` and exit 0 (not a failure)
- If API key present:
  - Call `GET https://api.upload-post.com/api/uploadposts/accounts` with x-api-key header
  - Verify response is 200 and returns an array (validates key works and accounts endpoint exists)
  - Log account count and platform names
  - Do NOT publish a test post (avoid spam) -- just validate the API key works and the accounts endpoint responds
  - Print `[PASS] Upload-Post API key valid, {N} accounts found`
  - Note in output: "Analytics endpoint validation deferred to Phase 7 -- requires a real published post with 48h analytics delay"
  </action>
  <verify>
    <automated>npx tsx scripts/validate/01-sqlite-wal.ts && npx tsx scripts/validate/02-better-sqlite3-nextjs.ts && npx tsx scripts/validate/03-cron-singleton.ts && npx tsx scripts/validate/04-satori-sharp.ts</automated>
  </verify>
  <done>All 4 non-API validation scripts exit 0 with [PASS] output. Script 05 exits 0 (either with API validation or [SKIP] message). SQLite WAL creates -wal/-shm files. better-sqlite3 round-trips data. Cron singleton fires exactly once across 3 register() calls. Satori+sharp produces a PNG > 1000 bytes.</done>
</task>

</tasks>

<verification>
Run all 5 validation scripts in sequence:
```bash
for f in scripts/validate/01-sqlite-wal.ts scripts/validate/02-better-sqlite3-nextjs.ts scripts/validate/03-cron-singleton.ts scripts/validate/04-satori-sharp.ts scripts/validate/05-upload-post.ts; do echo "=== $f ===" && npx tsx "$f" || exit 1; done
```
All scripts must exit 0. Scripts 01-04 must show [PASS] lines. Script 05 shows [PASS] or [SKIP].
</verification>

<success_criteria>
- Next.js 15 project scaffolded with all dependencies
- 5 validation scripts exist and run independently
- Scripts 01-04 pass locally with [PASS] output
- Script 05 gracefully skips if no API key, validates if key present
- No test frameworks -- simple standalone scripts per user decision
</success_criteria>

<output>
After completion, create `.planning/phases/00-infrastructure-validation-spike/00-01-SUMMARY.md`
</output>
