---
phase: 00-infrastructure-validation-spike
plan: 02
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - Dockerfile
  - instrumentation.ts
  - src/app/page.tsx
  - src/app/api/health/route.ts
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Next.js 15 + better-sqlite3 builds successfully on Railway (Railpack or Dockerfile)"
    - "SQLite database on Railway volume mount persists across redeploy"
    - "node-cron fires via instrumentation.ts and tick appears in Railway logs"
    - "Satori+sharp renders PNG on Railway's Linux environment"
  artifacts:
    - path: "Dockerfile"
      provides: "Railway deployment config with native module build tools"
    - path: "instrumentation.ts"
      provides: "Cron singleton with NEXT_RUNTIME guard"
    - path: "src/app/api/health/route.ts"
      provides: "Health endpoint that validates SQLite + Satori on Railway"
  key_links:
    - from: "instrumentation.ts"
      to: "node-cron"
      via: "dynamic import inside register()"
      pattern: "await import.*node-cron"
    - from: "src/app/api/health/route.ts"
      to: "better-sqlite3"
      via: "opens DB on Railway volume"
      pattern: "RAILWAY_VOLUME_MOUNT_PATH"
---

<objective>
Deploy the minimal Next.js app to Railway and validate that SQLite WAL, better-sqlite3, node-cron, and Satori+sharp all work in Railway's Linux environment.

Purpose: Confirm Railway-specific infrastructure assumptions (volume mount, native module builds, cron in production) that cannot be validated locally.
Output: Successful Railway deployment with all 4 Railway-dependent validations confirmed.
</objective>

<execution_context>
@C:/Users/simon/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-infrastructure-validation-spike/00-CONTEXT.md
@.planning/phases/00-infrastructure-validation-spike/00-RESEARCH.md
@.planning/phases/00-infrastructure-validation-spike/00-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Railway deployment files and health endpoint</name>
  <files>Dockerfile, instrumentation.ts, src/app/api/health/route.ts, src/app/page.tsx</files>
  <action>
Create the files needed to deploy and validate on Railway:

**Dockerfile** (fallback if Railpack fails with better-sqlite3 node-gyp):
```dockerfile
FROM node:22-slim AS base
RUN apt-get update && apt-get install -y python3 make g++ && rm -rf /var/lib/apt/lists/*

FROM base AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

FROM base AS runner
WORKDIR /app
ENV NODE_ENV=production
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static ./.next/static
COPY --from=builder /app/public ./public

EXPOSE 3000
ENV PORT=3000
CMD ["node", "server.js"]
```

NOTE: First try deploying WITHOUT the Dockerfile (let Railpack handle it). Only add Dockerfile if Railpack fails to build better-sqlite3. This is exactly the fallback strategy from CONTEXT.md.

**instrumentation.ts** (project root, NOT src/):
- Export async `register()` function
- Guard: `if (process.env.NEXT_RUNTIME !== 'nodejs') return`
- Guard: `if ((globalThis as any).__cronRegistered) return` with log
- Set `(globalThis as any).__cronRegistered = true`
- Lazy import node-cron: `const { default: cron } = await import('node-cron')`
- Schedule a tick every minute: log `[cron] tick at {ISO timestamp}`
- Log `[cron] Scheduler registered`

**next.config.ts update:**
- Add `output: 'standalone'` (required for Docker deployment on Railway)

**src/app/api/health/route.ts:**
Create a GET endpoint that validates all 4 Railway-specific concerns and returns JSON:

```typescript
// GET /api/health — validates infrastructure on Railway
// Returns { status, checks: { sqlite, satori, cron, volume } }
```

Checks:
1. **sqlite**: Open DB at `RAILWAY_VOLUME_MOUNT_PATH/app.db` (or `./data/app.db` locally), set WAL mode, run integrity check, insert+query a test row. Report pass/fail.
2. **satori**: Render a minimal Satori element to SVG, convert with sharp to PNG buffer. Report pass/fail and PNG size in bytes.
3. **cron**: Check `(globalThis as any).__cronRegistered` flag. Report whether cron was registered.
4. **volume**: Check if `RAILWAY_VOLUME_MOUNT_PATH` env var is set and the directory exists. Report path and writable status.

Return 200 with all results as JSON. This lets us validate everything with a single curl call after deploy.

**src/app/page.tsx:**
Simple page that says "Infrastructure Validation Spike" with a link to `/api/health`. This is the landing page -- minimal, just confirms the app is running.
  </action>
  <verify>
    <automated>npm run build && curl -s http://localhost:3000/api/health 2>/dev/null || echo "Build succeeded (server test requires running app)"</automated>
  </verify>
  <done>Dockerfile created. instrumentation.ts with cron singleton created. Health endpoint validates all 4 Railway concerns. next.config.ts has output: 'standalone'. App builds locally without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Railway deployment and infrastructure validation</name>
  <action>
Deploy the app to Railway and verify all infrastructure assumptions work in the Railway Linux environment. The user must create the Railway project, attach a volume, and deploy.
  </action>
  <what-built>
Railway deployment with infrastructure validation. The app includes:
- A health endpoint (`/api/health`) that validates SQLite WAL, Satori+sharp, cron registration, and volume mount
- instrumentation.ts with cron singleton that logs ticks every minute
- Dockerfile fallback if Railpack can't build better-sqlite3

You need to:
1. Create a Railway project (if not already done)
2. Attach a volume mounted at `/data`
3. Set env var `RAILWAY_VOLUME_MOUNT_PATH=/data`
4. Connect the GitHub repo and deploy
  </what-built>
  <how-to-verify>
1. **Deploy to Railway:**
   - Go to Railway dashboard, create new project from GitHub repo
   - First try WITHOUT Dockerfile (delete or rename it) -- let Railpack build
   - If Railpack build fails on better-sqlite3, re-add Dockerfile and redeploy
   - Add a volume mounted at `/data`
   - Set env var: `RAILWAY_VOLUME_MOUNT_PATH=/data`

2. **Check build logs:**
   - Verify `npm install` succeeds (better-sqlite3 native module compiles)
   - Verify `npm run build` succeeds (Next.js builds without bundling errors)
   - Note whether Railpack or Dockerfile was used

3. **Check runtime:**
   - Visit `https://{your-railway-url}/api/health`
   - All 4 checks should show pass: sqlite, satori, cron, volume
   - Visit Railway logs -- look for `[cron] Scheduler registered` and `[cron] tick at` messages (wait 1-2 minutes for a tick)

4. **Test persistence:**
   - Redeploy the app (Railway dashboard > redeploy)
   - After redeploy, visit `/api/health` again
   - SQLite check should still pass (volume persisted the DB file)
   - Cron should re-register (new process)

5. **Report results:**
   - Which builder worked (Railpack or Dockerfile)?
   - Did all 4 health checks pass?
   - Did cron tick appear in logs?
   - Did data persist across redeploy?
  </how-to-verify>
  <verify>User confirms all 4 Railway checks pass via /api/health endpoint</verify>
  <done>All 4 Railway-specific infrastructure assumptions validated: build succeeds, SQLite WAL works on volume, cron ticks in logs, Satori+sharp renders PNG.</done>
  <resume-signal>Report the Railway deployment results: which builder worked, health check output, cron log presence, persistence across redeploy. Type "all passed" if everything works, or describe any failures so we can activate fallbacks.</resume-signal>
</task>

</tasks>

<verification>
All 4 Railway-specific validations confirmed:
1. `GET /api/health` returns all checks passing
2. Railway build log shows successful native module compilation
3. Railway runtime logs show `[cron] tick` messages
4. Data persists across redeploy (volume mount working)
</verification>

<success_criteria>
- Next.js 15 + better-sqlite3 builds on Railway (Railpack or Dockerfile fallback)
- SQLite WAL mode works on Railway volume, data persists across redeploy
- node-cron registers once via instrumentation.ts, ticks appear in logs
- Satori+sharp renders PNG on Railway's Linux environment
- All validations documented for future reference
</success_criteria>

<output>
After completion, create `.planning/phases/00-infrastructure-validation-spike/00-02-SUMMARY.md`
</output>
